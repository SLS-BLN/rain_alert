# Environment configuration for Rain Alert Script
# Fill in your API credentials and location details below.
# Do not remove required keys — the script depends on them.

# OpenWeatherMap
API_KEY=your_openweathermap_api_key
API_ENDPOINT=https://api.openweathermap.org/data/2.5/forecast

# Geolocation Data
LATITUDE=52.5200
LONGITUDE=13.4050

# Forecast configuration
# OpenWeatherMap provides forecast data in fixed 3-hour intervals — this cannot be changed.
# FORECAST_HOURS defines how far ahead to check for rain alerts.
# IMPORTANT: FORECAST_HOURS must be divisible by 3 (e.g. 6, 12, 24) to match the API's interval structure.
# The number of forecast timestamps is calculated as: cnt = FORECAST_HOURS / 3
FORECAST_HOURS=12

# Twilio
TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
MY_PHONE_NUMBER=+0987654321
